// PageTransition component type definitions

import { ReactNode } from "react";

/**
 * Props for page transition component
 */
export type PageTransitionProps = {
  /** Child components to wrap with transition */
  children: ReactNode;
};
